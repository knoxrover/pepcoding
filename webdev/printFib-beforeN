function processData(input) {
    
    let a=0;
    let b=1;
    let sum=0;
    
    while(a<input){
        process.stdout.write(a+" ");
        sum=a+b;
        a=b;
        b=sum;
    }
    
    
} 
